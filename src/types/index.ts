export enum API_STATUS {
    INVALID_TOKEN = 422,
    MAINTENANCE = 503,
}

export enum USER_STATUS {
    ACTIVE = "ACTIVE",
    NON_ACTIVE = "NON ACTIVE",
}

export enum PLAYER_STATUS {
    TROUBLE = "TROUBLE",
    BLOCKED = "BLOCKED",
}